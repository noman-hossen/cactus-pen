{
  "name": "ai-paragraph-backend",
  "scripts": {
    "dev": "bun --env-file=.env run --watch src/index.ts",
    "start": "bun --env-file=.env run src/index.ts",
    "test": "bun test"
  }

  ,
  "dependencies": {
    "@hono/node-server": "^1.13.1",
    "dotenv": "^17.2.3",
    "hono": "^4.11.7",
    "openai": "^4.71.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.5.4"
  }
}
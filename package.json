{
  "name": "ai-paragraph-writer",
  "version": "1.0.0",
  "description": "Full-stack AI paragraph generation application",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "bun run dev --workspace=backend & bun run dev --workspace=frontend",
    "dev:backend": "bun run dev --workspace=backend",
    "dev:frontend": "bun run dev --workspace=frontend",
    "build": "bun run build --workspace=backend && bun run build --workspace=frontend",
    "start": "bun run start --workspace=backend"
  },
  "keywords": [
    "ai",
    "content-generation",
    "paragraph-writer",
    "vue",
    "hono",
    "typescript"
  ],
  "author": "",
  "license": "MIT"
}
